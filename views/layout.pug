doctype html
html
  head
    title #{title}
    Link(href='style.css' rel='stylesheet')
  body
    div(class='navbar-menu')
      nav
        a(id='home', class="navbar-item", href="/") Home
        if loggedIn
          a(id='admin', class="navbar-item", href="/admin") Admin
          a(id='edit', class="navbar-item", href="/edit") Account
        else
          a(id='login', class="navbar-item", href="/login") Login
          a(id='register', class="navbar-item", href="/register") Register



    header
     h1(style="text-align: center;") #{title} 
  block content
footer